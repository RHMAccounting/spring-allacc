spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RHMAccounting/RHMConfig
          search-paths:
            - "{application}/{profile}"
            - "global/{profile}"